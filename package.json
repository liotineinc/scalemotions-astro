{
  "name": "scalemotions-astro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "format": "prettier --write ."
  },
  "dependencies": {
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
