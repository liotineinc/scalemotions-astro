---
interface Props {
  number?: string;
  label: string;
}

const { number, label } = Astro.props;
---

<div class="section-label micro">
  {number && <span>{number}</span>}
  <span>{label}</span>
</div>

