---
import BaseLayout from "../layouts/BaseLayout.astro";
import LegalContent from "../components/LegalContent.astro";
import { termsContent } from "../data/legal";
import { breadcrumbSchema, organizationSchema } from "../data/schema";

const schema = [organizationSchema(), breadcrumbSchema([{ name: "Home", path: "/" }, { name: "Terms", path: "/terms" }])];
---

<BaseLayout
  title="Terms & Messaging Terms - Scale Motion LLC"
  description="Terms and SMS messaging terms for Scale Motion LLC, including customer-care messages, opt-in, STOP, HELP, and mobile-data sharing disclosures."
  canonical="/terms"
  schema={schema}
>
  <LegalContent {...termsContent} />
</BaseLayout>

